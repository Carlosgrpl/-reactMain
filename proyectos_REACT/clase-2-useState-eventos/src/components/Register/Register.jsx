function Register ({ title, texto, textMessage }) {

    const { msg, error } = textMessage;
    const { iser, setUser} = useState({

    
    })



const handleMode = (ev) => {
    const id = ev.target.id;
};
    return (
        <div>
        <button>
        Claro</button>
        <button>
        Oscuro</button>
        <h5>{title}</h5>
        <p>{msg}</p>
        <form action="">
        <label htmlFor="email"<eMial</label>
        <input type="text" name="" id="emai"/>
        </div>
    )